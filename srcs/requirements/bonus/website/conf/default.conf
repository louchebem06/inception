server
{
	listen 8888;
	listen [::]:8888;

	server_name ${DOMAIN_NAME};

	root /var/www/website;
	index index.html;

	location /
	{
		try_files $uri $uri/ /index.html;
		autoindex on;
	}
}
